function reverseVowels(s){
    let i=0;
    len=s.length;
    s=s.toLowerCase();
    while (i<len){
        if(s[i]=="a" || s[i]=='e' || s[i]=='i' ||s[i]=='o' ||s[i]=='u'){
            
        }
    }
}